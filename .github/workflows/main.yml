on:
  push:
    branches:
      - main

name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Create directories on server
      run: |
        lftp -u 4206038,8fHRr@2x1cC_24pE -e "mkdir -p /home/www/css; mkdir -p /home/www/img; mkdir -p /home/www/img/menu; mkdir -p /home/www/img/products; mkdir -p /home/www/js; exit" mahyudin.cloud
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: mahyudin.cloud
        username: 4206038
        password: 8fHRr@2x1cC_24pE
        remote-dir: /home/www
